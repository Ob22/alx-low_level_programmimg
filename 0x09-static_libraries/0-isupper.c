#include "main.h"
#include <stdio.h>

/**
* _isupper - function that checks for uppercase character
* @c: input
* Return: 1 if character is uppercase, otherwise 0.
*/

int _isupper(int c)
{
	return (0);
}
